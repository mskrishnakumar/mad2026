# =============================================================================
# Azure Configuration - Hackathon Environment
# =============================================================================
# Update these values for your hackathon Azure account
# =============================================================================

# Resource Group
RESOURCE_GROUP="mad2026-hackathon-rg"

# Azure Region (see: az account list-locations -o table)
LOCATION="eastus"

# Azure Container Registry (must be globally unique, lowercase, alphanumeric only)
ACR_NAME="mad2026hackathonacr"

# Container Apps Environment Name
CONTAINER_APP_ENV="mad2026-hackathon-env"

# Container App Name
CONTAINER_APP_NAME="mad2026-hackathon-app"

# Docker Image Name (without registry prefix)
IMAGE_NAME="mad2026"

# =============================================================================
# Environment Variables (Optional)
# =============================================================================
# Add environment variables for your container app
# Format: "KEY1=value1 KEY2=value2"
# For secrets, use Azure Key Vault or set them separately via Azure Portal
#
# Example:
# ENV_VARS="NEXT_PUBLIC_APP_NAME=MAD2026-Hackathon NODE_ENV=production"
#
ENV_VARS="NEXT_PUBLIC_APP_NAME=MAD2026-Hackathon"
